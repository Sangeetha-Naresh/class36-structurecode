class Player
 {
    constructor(){}
  
    getCount()
    { 
      var playerCountRef = database.ref('      ');
      
     
      playerCountRef.on("value",function(data)
                                {
                                     playerCount =
                                });
      
     
     
    }
  
    updateCount(count)
    {
      database.ref('/').update(                                 );
    }
  
    updateName(name)
    {
      var playerIndex = "player" + ;
      database.ref(                                 ).set(                               );
    }
  }
