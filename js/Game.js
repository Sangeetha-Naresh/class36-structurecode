class Game
{
    constructor() {}

    getState()
    {
        
        var gameStateRef= database.ref('                     ');
        
        gameStateRef.on("value",function(data)
                                {
                                    gameState = 
                                });
                                
         
    }

    update(state)
    {
        database.ref('/').update(                           );
    }   

    start()
    {
        if(gameState === 0)
        {
         
        }
    }

}